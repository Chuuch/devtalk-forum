import { SocialIcon } from 'react-social-icons';

const Card = () => {
	return (
		<article
			className="flex flex-col rounded-lg items-center space-y-7 justify-start flex-shrink-0
    w-[500px] bg-gray-800"
		></article>
	);
};

export default Card;
